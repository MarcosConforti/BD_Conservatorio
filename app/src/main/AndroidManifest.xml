<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bdconservatorio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BDConservatorio">
        <activity android:name=".GestionarAsignaturas"></activity>
        <activity android:name=".InformacionUsuario" />
        <activity android:name=".Configuracion" />
        <activity android:name=".EnviarReporte" />
        <activity android:name=".Calendario" />
        <activity android:name=".AcercaDe" />
        <activity android:name=".Asignaturas" />
        <activity android:name=".Alumnos" />
        <activity android:name=".Maestros" />
        <activity android:name=".Registro" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>